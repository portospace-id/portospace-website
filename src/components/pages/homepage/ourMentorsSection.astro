---
import { Image } from "astro:assets";
import SectionTitle from "@/components/ui/sectionTitle.astro";
import CardMentor from "@/components/card/cardMentor.astro";

import PatternLine from "@/components/ui/patternLine.astro";
import testimonial4Img from "@/assets/image/homepage/testimonial-4.webp";
import testimonial5Img from "@/assets/image/homepage/testimonial-5.webp";
import testimonial3Img from "@/assets/image/homepage/testimonial-3.webp";

const mentors = [
  {
    id: "mentor-1",
    name: "Farhan M.",
    field: "Biotechnology",
    university: "Institut Teknologi Bandung",
    image: testimonial4Img,
  },
  {
    id: "mentor-2",
    name: "Farhan M.",
    field: "Biotechnology",
    university: "Institut Teknologi Bandung",
    image: testimonial4Img,
  },
  {
    id: "mentor-3",
    name: "Farhan M.",
    field: "Biotechnology",
    university: "Institut Teknologi Bandung",
    image: testimonial4Img,
  },
  {
    id: "mentor-4",
    name: "Farhan M.",
    field: "Biotechnology",
    university: "Institut Teknologi Bandung",
    image: testimonial4Img,
  },
  {
    id: "mentor-5",
    name: "Jasmine Shafa, M.Sc.",
    field: "STEM Research",
    university: "Universitas Indonesia",
    image: testimonial5Img,
  },
  {
    id: "mentor-6",
    name: "Aris Putra, Ph.D.",
    field: "Coding & AI",
    university: "Nanyang Technological University",
    image: testimonial3Img,
  },
];
---

<section
  id="our-mentors"
  class="relative w-full bg-violet-normal py-[60px] lg:py-[100px] flex flex-col justify-center overflow-hidden"
  aria-label="Our Mentors"
  data-navbar="violet"
>
  <!-- Decorative Pattern SVG - Top Right -->
  <PatternLine
    class="absolute z-0"
    style="top: 0; right: -80px; width: 477px; height: 521px; display: block;"
  />

  <!-- Decorative Pattern SVG - Bottom Left -->
  <PatternLine
    class="absolute z-0 rotate-180"
    style="bottom: 0; left: -61px; width: 477px; height: 521px; display: block;"
  />

  <!-- Header Content -->
  <div
    class="relative z-10 w-full max-w-[1440px] mx-auto xl:max-w-none px-[20px] sm:px-[24px] md:px-[28px] lg:px-[32px] xl:px-[32px] flex flex-col items-center text-center gap-4 mb-10 lg:mb-14"
  >
    <div id="our-mentors-badge">
      <SectionTitle title="Our Mentor" />
    </div>

    <h2
      id="our-mentors-heading"
      class="font-momo font-semibold text-white text-[32px] md:text-[44px] lg:text-[54px] leading-[110%] tracking-[-1.08px] max-w-[980px]"
    >
      Belajar langsung dari mentor yang<br class="hidden md:block" />
      pernah membangun <span
        class="font-caveat font-medium italic text-white text-[42px] md:text-[54px] lg:text-[64px] leading-[80%] tracking-[-2.56px]"
        >prestasi global</span
      >
    </h2>

    <p
      id="our-mentors-desc"
      class="font-jakarta font-normal text-white/90 text-sm lg:text-base leading-[160%] max-w-[680px]"
    >
      PortoSpace mencocokkan siswa dengan mentor berpengalaman di riset, kompetisi,
      teknologi, dan persiapan universitas internasional.
    </p>
  </div>

  <!-- Cards Container / Horizontal Track -->
  <div
    id="our-mentors-cards-container"
    class="relative z-10 w-full overflow-x-auto no-scrollbar pb-4 cursor-grab active:cursor-grabbing select-none"
  >
    <div
      id="our-mentors-cards-track"
      class="flex items-center gap-5 w-max px-[20px] sm:px-[24px] md:px-[28px] lg:px-[32px] will-change-transform"
    >
      {
        mentors.map((m) => (
          <CardMentor
            name={m.name}
            field={m.field}
            university={m.university}
            image={m.image}
          />
        ))
      }
    </div>
  </div>
</section>

<style>
  /* Hide scrollbar for clean UI while maintaining native scrollability */
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
</style>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const initMentorsAnimations = () => {
    const section = document.getElementById("our-mentors");
    if (!section) return;

    gsap.from("#our-mentors-badge", {
      scrollTrigger: {
        trigger: section,
        start: "top 85%",
        once: true,
      },
      y: 20,
      opacity: 0,
      duration: 0.6,
      ease: "power2.out",
    });

    gsap.from("#our-mentors-heading", {
      scrollTrigger: {
        trigger: section,
        start: "top 80%",
        once: true,
      },
      y: 30,
      opacity: 0,
      duration: 0.8,
      ease: "power3.out",
      delay: 0.1,
    });

    gsap.from("#our-mentors-desc", {
      scrollTrigger: {
        trigger: section,
        start: "top 80%",
        once: true,
      },
      y: 20,
      opacity: 0,
      duration: 0.6,
      ease: "power2.out",
      delay: 0.2,
    });

    gsap.from(".mentor-card", {
      scrollTrigger: {
        trigger: "#our-mentors-cards-container",
        start: "top 90%",
        once: true,
      },
      y: 40,
      opacity: 0,
      duration: 0.7,
      stagger: 0.1,
      ease: "power3.out",
      clearProps: "transform,opacity",
    });

    // Interactive Drag to Scroll for Desktop
    const container = document.getElementById("our-mentors-cards-container");
    if (container) {
      let isDown = false;
      let startX = 0;
      let scrollLeft = 0;

      container.addEventListener("mousedown", (e) => {
        isDown = true;
        startX = e.pageX - container.offsetLeft;
        scrollLeft = container.scrollLeft;
      });

      container.addEventListener("mouseleave", () => {
        isDown = false;
      });

      container.addEventListener("mouseup", () => {
        isDown = false;
      });

      container.addEventListener("mousemove", (e) => {
        if (!isDown) return;
        e.preventDefault();
        const x = e.pageX - container.offsetLeft;
        const walk = (x - startX) * 1.5;
        container.scrollLeft = scrollLeft - walk;
      });
    }
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initMentorsAnimations);
  } else {
    initMentorsAnimations();
  }
</script>
